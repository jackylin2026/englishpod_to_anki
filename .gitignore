__pycache__/
*.py[cod]
*.egg-info/
.venv/
.mypy_cache/
.pytest_cache/

# The OCR service's key and secret, which are its owner's and nobody else's.
.env

# What the tool writes: one lesson to a directory, when run from the checkout.
/build/
